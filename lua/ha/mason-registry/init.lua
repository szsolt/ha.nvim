-- Home Assistant Mason registry index (v2 format)
-- This uses the table-based format instead of the function-based format

return {
    ["home-assistant-language-server"] = "ha.mason-registry.home-assistant-language-server",
}
